import 'package:flutter/material.dart';

class NastroScreen extends StatelessWidget {
  const NastroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}