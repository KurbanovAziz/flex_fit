import 'package:flutter/material.dart';

class MemorilyScreen extends StatelessWidget {
  const MemorilyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}